# 🛰 کتاب عملیات (Runbook) — DropAgentX

## استقرار Railway
1. Fork → پروژه جدید → Volume به `/app/data`
2. متغیرها: `BOT_TOKEN`، `ADMIN_IDS`، `FILE_STORAGE_CHANNEL_ID` (بقیه اختیاری)
3. Deploy → healthz سبز؟ `VERSION` را چک کن
4. بات را ادمین کانال ذخیره فایل کن

## روتین روزانه (۵ دقیقه)
- [ ] پنل `/admin` → 🩺 سلامت سیستم (حجم DB زیر ۴۰۰MB؟)
- [ ] 🎫 `/tickets` — تیکت‌های باز؟ `/trep ID متن`
- [ ] ⚖️ بررسی تسک‌ها → داوری کارت‌به‌کارت
- [ ] 💰 واریز/برداشت‌های معلق

## روتین هفتگی
- [ ] `/giveaway جایزه 5 7` — قرعه‌کشی فعال‌ها (وایرال)
- [ ] 👥 Users → 📤 بک‌اپ CSV مخاطبان
- [ ] 📈 رشد کاربران — افت؟ → `/idlemsg 3 متن` win-back
- [ ] 💸 درآمد ۳۰روز — روند؟

## فوریت‌ها
| علامت | اقدام |
|---|---|
| هشدار حجم DB | پنل → 🗜️ VACUUM · بعدش 🗄️ آرشیو تراکنش |
| کاربر: «فایل نیومد» | 📦 سلامت محصولات → کاندیدهای بدون‌فایل → انتقال ابر |
| ریفرال صفر گزارش | چک `start.py` is_new + استتوس تسک‌ها (fixed در v2+) |
| بات mute؟ | healthz → لاگ‌ها `/errmap` → TelegramBadRequestها info فقط |

## بک‌اپ
- خودکار: روزانه به چت ادمین (`BACKUP_TO_TELEGRAM=1`)
- دستی: SQLite → `sqlite3 data/marketplace.db ".backup backup.db"` (بدون داون‌تایم)
- مخاطبان: پنل → 📤 CSV (هفتگی)
