# DropAgentX — Changelog v3.5.0 «رشد»

تاریخ: ۲۰۲۶-۰۸-۲۸ · مبنای این ریلیز: **تحقیق بازار (رقبای تلگرامی) + ۵ قابلیت نگهداشت/کمپین که غایب بودند**

## 🔍 خلأهای پیدا شده از تحقیق بازار
مطالعهٔ رقبای مارکت‌پلیس/ربات‌های رشد تلگرام نشان داد: ابزارهای نگهداشت (بونوس روزانه/استریک)،
کد هدیه کمپینی برای تبلیغات، قرعه‌کشی، پیام به راکدها (win-back) و امتیازدهی در-بات در همهٔ
ربات‌های موفق هستند و در DropAgentX غایب بودند — اضافه شدند.

## 🎁 بونوس روزانه + استریک (نگهداشت)
- «💰 کیف پول → 🎁 بونوس روزانه» و «✅ کسب کردیت → 🎁»
- روز اول ۱۵ کردیت، هر روز استریک +۵، سقف ۵۰ (`DAILY_BONUS_BASE/STEP/CAP`)
- پنجرهٔ ۲۰ساعته (انعطاف کاربر واقعی) · استریک تا ۴۶ساعت زنده، بعد ریست
- دعوت خودکار در خوش‌آمد بازگشت وقتی بونوس آماده است + گارد رقابتی claim

## 🎟 کد هدیه کمپینی (ابزار تبلیغات)
- `/promonew CODE کردیت تعداد [روز]` · `/promolist`
- کاربر: «💰 کیف پول → 🎟 کد هدیه دارم» → کد → شارژ فوری
- ضد سوءاستفاده: UNIQUE(کاربر×کد) · سقف استفاده · انقضای اختیاری · بدون حساسیت بزرگی حروف
- **کاربرد لانچ:** توی پست تبلیغاتی کد بده (مثل `TELEGRAM50`) — جذب قابل‌اندازه‌گیری

## 🎉 قرعه‌کشی (وایرال)
- `/giveaway جایزه تعداد_برنده روزها` → انتخاب تصادفی از فعال‌های N روز اخیر
- پرداخت خودکار + DM تبریک به برنده‌ها + اعلام عمومی (حداکثر ۲۵ برنده)

## 💤 پیام به راکدها (win-back)
- ردیابی `last_seen` سبک (حداکثر ۱ نوشتن در ۱۵ دقیقه)
- `/idlemsg روزها متن` → حداکثر ۱۰۰۰ کاربر راکد + استثنا ادمین‌ها + گزارش موفق/شکست

## ⭐ امتیازدهی در-بات (اعتماد)
- بعد از تحویل هر فایل: دکمهٔ «⭐ امتیاز به این محصول» → ۱ تا ۵ ستاره
- فقط خریدار · upsert (تغییر نظر ممکن) · تیبل reviews موجود استفاده شد

## 🧭 پنل ادمین + راهنما
- ردیف جدید پنل: 🎟 کد هدیه · 🎉 قرعه‌کشی · 💤 راکدها (با راهنمای نمایشی)
- FAQ: ۲ سوال جدید (بونوس روزانه، کد هدیه) · راهنمای کامل: بخش بونوس روزانه

## ✅ اعتبارسنجی
- py_compile کل ریپو ✓ · pytest مونوریپو **۵۷/۵۷** ✓
- **۱۹/۱۹ تست عملکردی رشد** روی SQLite واقعی: چرخهٔ کامل بونوس/استریک/ریست، ۷ سناریوی کد هدیه،
  راکد/فعال، upsert امتیاز ✓
