# DropAgentX — Changelog v3.3.3 «غولِ پایدار»

تاریخ: ۲۰۲۶-۰۸-۲۸ · مبنای این ریلیز: **ادغام کامل v3.0.0 (مونوریپو) + لایه عملیاتی اثبات‌شده** — با ریسیمولیشن ظرفیت روی اسکیمای کامل جدید

## 🔀 این نسخه چیست؟

v3.0.0 معماری و هوش را جلو برد (گیت‌وی، روتر LLM، حافظه ۷فستی، RL، رصدپذیری) ولی
۶ باگ لاگ هنوز زنده بود و ظرفیت ۸۰۰۰ کاربر/۵۰۰MB را رد می‌کرد. v3.3.3 لایه عملیاتیِ
تست‌شده را روی همان معماری پیوند می‌زند — **بدون شکستن هیچ‌کدام از ۵۷ تست مونوریپو.**

## 🐛 فیکس باگ‌ها (از لاگ واقعی Railway)

- **FIX-1 · کرش دانلود محصول** — کپشن بدون سقف ۱۰۲۴ (`products.py` خط download) →
  کپشن امن HTML-escape + توضیحات تکه‌۴۰۹۶ + فالبک دولایه (HTML→plain) + بازخورد صریح به کاربر.
- **FIX-2 · parse_mode Markdown شکستنی** → HTML امن در مسیر محصول/دانلود + بازگرداندن `send_safe` در welcome.
- **FIX-3 · کرش بی‌بازخورد** → همه مسیرها `callback.answer(show_alert=True)`.
- **FIX-4 · پارس callback بدون گارد** → try/except + پیام.
- **FIX-5 · لاگ خطای سراسری `: None`** → رزولوشن سه‌منبعی exc (`exception` kwarg + `event.exception` + kwargs) — روی middleware/ErrorCapture خود v3 سوار شد.
- **FIX-6 · ریفرال همیشه صفر (۲ زیرباگ)** — `is_new` با `get_user` قبل از ساخت؛ واجد شرایطی هر دو استتوس `completed/verified`.
- **FIX-7 (جدید v3.3.3) · `starlette` جاافتاده در requirements.txt** — گیت‌وی روی دیپلوی تمیز کرش می‌کرد؛ اضافه شد (تست‌های gateway بدونش red بودند).
- **FIX-8 (جدید v3.3.3) · `config.VERSION` کرش می‌کرد** — VERSION ماژول-سطح بود ولی `admin_v2 (/system)` و `mcp_bridge` از روی آبجکت می‌خواندند → AttributeError. حالا اتریبیوت کلاس هم هست (gateway/a2a دیگر نسخه کهنه 3.0.0 هم نشان نمی‌دهند).
- **FIX-9 (جدید v3.3.3) · `cfg` در ریتین cron** — ارجاع به نام اشتباه باعث NameError ساکت (و کارنکردن ریتین) می‌شد.

## ⚖️ تسک‌ها — داوری گزینه‌به‌گزینه

- `confirm_task` → ثبت `pending` بدون پاداش فوری (ضد سوءاستفاده)
- پنل ادمین → ⚖️ کارت‌به‌کارت: ✅ تأیید (+پرداخت خودکار +DM +شمارش ریفرال) / ❌ رد (+آزادسازی ظرفیت) / ⏭ بعدی

## 📈 ظرفیت ۸۰۰۰ کاربر — اثبات روی اسکیمای کامل v3.3.3

شبیه‌ساز (`tools_capacity_sim.py` داخل ریپو) این‌بار **جدول‌های جدید مونوریپو را هم** دارد
(`app_logs`, `memory_facets`, `rl_identity`):

| سناریو | مصرف/کاربر | ۸۰۰۰ کاربر | سقف @500MB |
|---|---|---|---|
| واقع‌بینانه | ~۲۵ KB | **۱۹۵ MB ✅** | ~۲۰,۹۰۰ |
| AI-سنگین | ~۵۲ KB | **۴۰۹ MB ✅** (ناحیه هشدار ۴۰۰ — دیده‌شدن با هشدار روزانه، by design) | ~۹,۸۰۰ |

- **فایل ابری:** `FILE_STORAGE_CHANNEL_ID` → file_id تلگرام (۰ بایت Volume) + مایگریشن `products.file_fileid`
- **چت:** ۲۵ پیام · سقف نقشی ۲۰۰۰/۱۵۰۰ · FTS فقط کاربر
- **🆕 ریتین جداول v3 (ضد رشد بی‌سقف):** `prune_app_logs` (۱۴روز/۵۰k ردیف، کانفیگ‌پذیر)
  + `memory2.evict_expired` (TTL فستی‌ها) — هر دو در cron شبانه سیم‌کشی شدند؛ در v3.0.0 هیچ‌کدام وجود نداشت!

## 🛠 پنل ادمین — ۱۰ ابزار جدید (روی ابزارهای v3: admin_v2 هم سر جایش است)

⚖️ صف بررسی تسک‌ها · 🗄️ مانیتور دیتابیس (نوار ۵۰۰MB + تخمین ظرفیت) · 🧹 پاک‌سازی چت راکدها ·
🗜️ VACUUM · 📈 رشد · 🏆 فروشندگان برتر · 📦 سلامت محصولات · 💸 درآمد ۳۰ر · 🗄️ آرشیو تراکنش · 🩺 سلامت سیستم

## 🔧 نگهداری خودکار (cron)

هشدار حجم روزانه از `DB_WARN_MB` · sweep هفتگی چت راکدها (`SWEEP_DORMANT_DAYS`) ·
VACUUM ماهانه (`VACUUM_DAY`) · آرشیو اختیاری tx (`TX_ARCHIVE_DAYS`) · **🆕** ریتین app_logs + انقضای فستی‌ها

## ✅ اعتبارسنجی
- `pytest` مونوریپو: **۵۷/۵۷ passed** (پس از ادغام — بدون هیچ شکستنی)
- `py_compile` کل ریپو ✓
- ۱۳/۱۳ تست عملکردی لایه ظرفیت/داوری روی SQLite واقعی (با تیبل‌های v3) ✓
- شبیه‌سازی ظرفیت ۴k/۸k دو سناریو روی اسکیمای کامل ✓
- سوییپ نهایی: ۱ باگ رانتایم گرفته‌شده (`config`→`cfg` در ریتین cron)
