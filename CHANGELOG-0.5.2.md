# DropAgentX — Changelog 0.5.2 «QA عمیق»

تاریخ: ۲۰۲۶-۰۸-۲۹ · راند دوم ممیزی: اسکن تمام مسیرها، دکمه‌ها، جریان مالی و وب

## 🐛 فیکس‌های واقعی (از QA عمیق)
1. **تلهٔ لاگین HTTP:** کوکی ادمین به‌طور پیش‌فرض `Secure` بود → روی
   `http://IP:8080` مرورگر کوکی را دور می‌ریخت و لاگین پنل بی‌پایان می‌شد.
   حالا پیش‌فرض ۰؛ پشت TLS فقط `COOKIE_SECURE=1` بده (در راهنمای استقرار هست).
2. **route گمشدهٔ `/live`:** فایل داشبورد زنده هست بود ولی مسیر نداشت → 404.
   الان `/live` (با محافظت ادمین) سرو می‌شود.
3. **وب-تنها با دیتابیس خالی → ۵۰۰:** `build_app` حالا در استارتاپ `init_db`
   idempotent اجرا می‌کند — وب هیچ‌وقت روی DB خالی نمی‌سوزد.

## 🔍 ممیزی‌هایی که پاس شد (بدون باگ)
- **۱۳۸ دکمهٔ بات: صفر دکمهٔ مرده** (اسکن callback_data در برابر ==/startswith/in_)
- **جریان مالی:** hold بی‌کردیت رد می‌شود؛ خرید idempotent (خرید تکراری → خطا)؛
  کمیشن plan-based (free 15% / pro 5%)؛ رکورد purchases و ریویو ثبت می‌شود
- کوپن‌ها: انقضا/سقف مصرف اتمی ✓ · فروشندهٔ مسدود/محصول خودت رد می‌شود ✓
- sw.js: precache سالم، آیکون‌های manifest همه موجود، کش → dax-v0.5.2
- لاگین: رمز غلط 401، rate-limit، هش HMAC ✓

## ✅ QA
**۷۲/۷۲ تست** (+۴ رگرسیون برای همین فیکس‌ها) · کامپایل کل ✓ · healthz = 0.5.2 ✓
