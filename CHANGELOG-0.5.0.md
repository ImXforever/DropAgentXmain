# DropAgentX — Changelog 0.5.0 «بتای عمومی»

تاریخ: ۲۰۲۶-۰۸-۲۸ · **شروع شماره‌گذاری جدید (semver 0.x = پیش از پایداری ۱.۰)** — نسخهٔ مهاجرت VPS و لانچ واقعی

## 🐛 دو ایراد واقعی که ممیزی بتا گرفت
- **`/landing` اصلاً route نداشت!** صفحهٔ لندینگ سه‌بعدی فقط به‌صورت فایل بود و از وب‌سرور
  ۴۰۴ می‌داد → مسیر `GET /landing` اضافه شد (تست‌شده: ۲۰۰).
- **`VERSION` در کد هنوز 4.0.0 بود** (دو ریلیز وب عقب مانده بود) → حالا `0.5.0` و
  `healthz` همین را گزارش می‌دهد: `{"version": "0.5.0"}`

## 🧲 یوزرنیم بات خودکار (حذف ریسک پیکربندی دستی)
- `landing.html` و `showcase3d.html` حالا بعد از لود، `BOT_USERNAME` را از
  `/api/pub/info` می‌خوانند و همهٔ دکمه‌های CTA را خودکار به بات واقعی وصل می‌کنند
  (با حفظ پارامترهای ردیابی مثل `?start=landing100` و `?start=shop3d_N`).
- یعنی روی VPS فقط `.env` را ست کن — هیچ ویرایش فایلی لازم نیست.

## 🔍 ممیزی سازگاری (پاک)
- `FILE_STORAGE_CHANNEL_ID`: کانفیگ ↔ `.env.example` ↔ داکیومنت‌ها ✓ یکسان
- `SUPPORT_CONTACT=@ImXforevr`: ۱۴ رفرنس سازگار ✓
- `BOT_USERNAME` برای CTA فروشگاه ✓

## 🆕 کوچک‌ها
- `/version` — هر کاربری نسخهٔ دقیق بات را ببیند (شفافیت بتا)
- `sw.js` کش جدید `dax-v0.5.0` (cache-busting برای نصب‌های قبلی PWA)

## ✅ QA بتا (همه سبز)
py_compile کل ریپو ✓ · pytest **۶۲/۶۲** ✓
مسیرها: `/` `/shop` `/landing` `/showcase3d` = ۲۰۰ ✓ · SSE بدون لاگین = ۴۰۱ ✓
healthz = نسخه 0.5.0 ✓

## 🚀 نقشهٔ بعد از بتا (به سمت 1.0)
- [ ] استقرار VPS + HTTPS (PWA فعال می‌شود)
- [ ] کمپین ۱٬۰۰۰ کاربر با کد هدیه
- [ ] ۱.۰.۰ = پس از اولین هفتهٔ پایدار بدون کرش + ۵۰۰ کاربر فعال
