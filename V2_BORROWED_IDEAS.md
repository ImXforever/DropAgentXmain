# 🧭 تحلیل سه ریپوی «الهامبخش» و چیزهایی که ارزش بیرونکشیدن دارد

> مقایسه: `hermes-agent` (شرکت Nous)، `radius-hermes-railway-template`، و `9router`.
> همراه با **درصد شباهت پروژهی ما** به هر کدام، و **لیست دقیق** اینکه چه چیزی را باید به بات اضافه کنیم.

---

## ۱) خلاصهی هر ریپو

| | hermes-agent | radius-hermes-railway-template | 9router |
|---|---|---|---|
| **چیست** | ایجنت خودمختار عمومی (Tool-use، مهارتها، حافظه، مرورگر، چندپلتفرم) | قالب استقرار Hermes روی Railway (worker + Volume + کشف ایجنت) | روتر AI و صرفهجویی توکن (پروکسی OpenAI-compatible + داشبورد) |
| **حجم** | ~10,500 فایل (خیلی بزرگ/بامنبع) | ۶۹ فایل (سبک/قالب) | ~1,480 فایل (Next.js + proxy) |
| **زبان** | Python (سبک Hermes/Claude) | Python + Dockerfile | TypeScript/Next.js + proxy |
| **هدفِ اصلی** | یک ایجنت همهکاره | استقرار سریع + کشف/هویت ایجنت | کمکردن هزینهی LLM + فالبک |

---

## ۲) درصد شباهت (تخمین صادقانه، تفکیکشده)

> شباهت را در **دو محور** جدا میکنم: (الف) **لایهی «ایجنت/هوش مصنوعی»**، (ب) **لایهی «دامنه/محصول»**. چون پروژهی ما هم ایجنت است هم مارکتپلیس تلگرام.

### به hermes-agent → حدود **۶۵٪** (بالا در لایهی ایجنت، پایین در دامنه)
- **همپوشانی بالا (`~70–75%`)** در زیرساخت ایجنت. پروژهی ما عملاً «از سبک Hermes» الهام گرفته و همینها را بازخلق کرده:
  - `hermes_engine.py` → همان حالتهای `cli / http / api` هرمس
  - `skills.py` → فرمت `SKILL.md` هرمس
  - `memory.py` → provider های حافظه (سبک هرمس)
  - `approval.py` → همان لایههای تأیید (shell/file/url/audit) هرمس
  - `file_tools.py`, `sandbox.py`, `browser_tools.py`, `webtools.py`, `session_store.py` (FTS5) → همان ابزارهای هرمس
  - `fleet.py` → تیم چندایجنت
- **کم** در دامنهی محصول (`~30%`): هرمس ایجنت عمومی است؛ ما **مارکتپلیس + اقتصاد دوتایی + Mini App + ریفرال MLM + ارگان فرکتالی** داریم که هیچکدام در هرمس نیست.

**گرفته.** ما «همهی مفاهیم اصلی هرمس» را داریم، ولی با **آبِ خودمان** و برای **دامنهی خودمان**. پس شباهتِ واقعی این است: «۶۵٪ همخانواده در مغز، اما فرزندِ متفاوت در چهره و کار».

### به 9router → حدود **۳۰–۳۵٪**
- همپوشانی فقط در «فراخوانی LLM + فالبک بین ارائهدهندهها + کش پرامپت».
- ما `hermes_engine` (retry/backoff/fallback) و `prompt_cache.py` داریم؛ 9router هم `fallback + quota + فرمتترجمه + RTK`.
- اما 9router یک **پروکسی مستقل Next.js** است، در حالی که ما **تصمیمسازی + کشیدن tool-output** را داخل بات داریم. پس شباهت در رویکرد است، نه در پیادهسازی.

### به radius template → حدود **۴۰٪**
- همپوشانی: هر دو **Dockerfile + Railway deploy + Volume + لاگ JSON ساختاریافته لمی** (که تازه در v2.0.0 برای خودمان ساختیم).
- تفاوت: radius **کشف/هویت ایجنت** دارد (ERC-8004، `.well-known/*`, A2A agent card) که ما نداریم.

---

## ۳) چه چیزی ارزش بیرونکشیدن دارد (اولویتبندیشده)

### 🥇 خروجیِ ارزشمند — «صرفهجویی توکن» (از 9router → RTK) ✨
**بهترین ROI برای پول.** 9router میگوید فشردهسازی `tool_output` (خروجیهای `git diff / grep / find / ls / tree / log`) پیش از ارسال به LLM، **۲۰–۴۰٪ توکن ورودی** را ذخیره میکند — بدونِ ازدسترفتن دقت (lossless).

**چه چیزی بسازیم:** یک `token_saver` سبک که:
- خروجیِ ابزارها را پِیک کند، الگو را تشخیص دهد (`git-diff`, `grep`, `find`, `ls`, `tree`, `log`, `numbered`, `search-list`).
- هر بلوک را **کمی** فشرده کند (حذف خطوط تکراری/وقفههای طولانی/خطاهای پرشمار)، نه تَخلیهی محتوا.
- **امن و fail-safe**: اگر فشردهسازی سریعتر بکشد یا بزرگتر بشود، **خروجی اصلی** را نگه دارد — هرگز درخواست را نمیشکند.
- در `hermes_engine`/`chat_with_tools` هوک شود (قابل toggle).

> این را برایت **بهعنوان ماژول واقعی و تستشده** ساختم (بخش ۵ پایین). بقیهی 9router (quota tracking، multi-account round-robin، فرمتترجمه، dashboard) را بهعنوان فاز بعدی در نظر بگیر.

### 🥈 خروجیِ ارزشمند — «مهارتها با اصالتسنجی و امنیت» (از hermes → skills_hub)
نسخهی ما `skills_catalog.py` دارد، ولی هرمس `skills_hub.py` با اینها جلوتر است:
- **Provenance/Lock file** (`HubLockFile`) → بدانی هر مهارت از کجا آمده و چه هشی دارد.
- **Skill Guard** (`skills_guard.py`, `threat_patterns.py`, `plugin_guard.py`) → اسکن محتوای مهارت/پلاگین برای الگوهای خطرناک، و **قرنطینه**.
- **Skill Ledger** (`skill_ledger.py`) → ثبت استفادهی هر مهارت.
- **محدودسازی خروجی ابزار** (`tool_output_limits`, `tool_result_storage`).

**چه چیزی بگیریم:** `skills_guard` + `HubLockFile` + `skill_ledger` را به `skills_catalog`/`skills.py` خودمان اضافه کنیم. این **مستقیم میوهی «امنیت»** میدهد، چون مهارتهای قابل نصب، خطرناکترین نقطهی افزودنی کد در بات است.

### 🥉 خروجیِ ارزشمند — «فشردهسازی تاریخچه/تراژکتوری» (از hermes → trajectory_compressor)
هرمس `trajectory_compressor.py` دارد: وقتی گفتگو (بهخصوص در chat_with_tools/فلیت) طولانی میشود، **پیچهای میانی** را با یک خلاصهی واحد جایگزین میکند و **اول و آخر را حفظ** میکند تا مدل ادامه دهد.
- برای ما یعنی: کمکردن هزینهی چتهای بلند با هرمسا + جلوگیری از overflow کانتکست.
- **راحتتر و کمخطرتر از نوشتن هوش جدید**؛ میتوانیم همین الگو را برای truncate-history در `ai_chat` بهکار ببریم.

### 🏅 خروجیِ ارزشمند — «کشف و هویت ایجنت + A2A» (از radius)
`radius` اینها را دارد که برای «دنیا بفهمد باتِ ما یک ایجنت است» مفید است:
- **`.well-known/*`** → ثبت ERC-8004 + **A2A agent card** (agent.json).
- **A2A communication** → `message/send`, `message/stream`, و حالت delegated (webhook).
- **هویت رمزنگاریشدهی دائمی** → از keystore کیف پول، JWT و امضای on-chain.
- **الگوی `requiredMountPath="/data"`** → همان چیزی که در `DEPLOY_V2-railway.md` گفتم (الزام Volume).

**چه چیزی بگیریم:** «A2A agent card + `.well-known`» و الگوی Volume. بقیه (ERC-8004، رجیستری radius) به دردِ دامنهی ما نمیخورد مگر بخواهیم بخشی از اکوسیستم radius شویم.

---

## ۴) جمعبندی «چه چیزی را بیرون بکشیم و به بات اضافه کنیم»

| فیچر | از کجا | دلیل | دشواری | اولویت |
|---|------|------|:---:|:---:|
| 🪙 **Token Saver** (فشردهسازی tool output) | 9router/RTK | صرفهجویی مستقیم ۲۰–۴۰٪ هزینه | کم | 🔥 خیلی بالا |
| 🛡 **Skill Guard + Provenance + Ledger** | hermes | امنسازی مهارتهای قابل نصب | متوسط | بالا |
| 🧠 **Trajectory Compressor** | hermes | کمکردن هزینهی چتهای بلند + کنترل کانتکست | کم | بالا |
| 🪪 **A2A Agent Card + `.well-known`** | radius | کشفپذیری بات بهعنوان ایجنت | متوسط | متوسط |
| 🌐 **A2A messaging (direct + delegated)** | radius | گفتوگوی ماشین-به-ماشین | متوسط | متوسط |
| 📈 **Quota Tracking + Multi-account round-robin** | 9router | مدیریت سهمیه و چندکلیدی | متوسط | آینده |
| 🤝 **Formato Translation (OpenAI↔Claude↔Gemini)** | 9router | سازگاری یکپارچه با هر ابزار | متوسط | آینده |

---

## ۵) نمونهی پیادهسازی — Token Saver (خروجیِ RTK/9router) ✅

یک ماژول سبک، امن و **fail-safe** میسازم که خروجیِ ابزارها را پیش از LLM فشرده میکند.

**قواعد ایمنی (مثل RTK):**
- اگر بلوک کمتر از آستانه باشد → دست نمیزنم.
- اگر الگو را تشخیص ندادم → دست نمیزنم.
- اگر فشردهسازی «بزرگتر» از ورودی شد → **ورودی اصلی** برمیگردد.
- هرگز استثنا پرتاب نمیکند (خروجی main در هر خطا).

### بذار ماژول و تستش را بسازم و اجرا کنم.
